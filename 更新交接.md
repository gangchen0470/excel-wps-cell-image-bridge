# 更新与交接记录

## 2026-09-15 — MVP 共享核心首版

### 影响范围

- 新建 Python OOXML 核心；项目此前没有代码，无可复用文件。
- 未修改用户提供的 Excel/WPS 原始样本。
- 不依赖 Excel、WPS 或第三方 Python 包。

### 已完成

- 识别 Excel Rich Data 单元格图片映射链。
- 识别 WPS `DISPIMG` 与 `cellimages.xml` 映射链。
- 建立统一 `CellImage` 数据模型。
- 将两种单元格图片转换为标准 DrawingML 浮动图片。
- 直接复用原始媒体数据，不重新编码或压缩图片。
- 按单元格尺寸等比例缩放、居中，默认边距 3 px。
- 拒绝覆盖输入文件；输出采用临时文件完成后原子替换。
- 增加真实样本检测与转换测试。

### 验证样本

- `excle test.xlsx`：识别 Excel 单元格图片 1 张，位置 `Sheet1!A1`。
- `wps test.xlsx`：识别 WPS 单元格图片 1 张，位置 `Sheet1!A1`。
- 两个转换结果均通过 ZIP CRC、Drawing、relationship、源格式索引移除检查。
- 两个兼容版均已由 LibreOffice Calc 实际载入并导出 PDF，图片在 `A1` 单元格范围内等比例显示。

### 后续优先级

1. 用 Microsoft Excel 与 WPS 两个目标客户端分别打开生成文件，确认无修复提示。
2. 增加原生 Excel → WPS 和 WPS → Excel 写回器。
3. 增加范围筛选、批量进度、逐图错误日志。
4. 覆盖合并单元格、隐藏行列、已有 Drawing、多图及损坏引用样本。

## 2026-09-15 — Excel 工作表 XML 修复

### 问题

- Excel 报告 `xl/worksheets/sheet1.xml` 第 2 行第 376 列解析错误并替换工作表。
- 原因是标准库 XML 序列化时改写了 Office 命名空间前缀，而 `mc:Ignorable` 属性仍引用旧前缀，其中部分前缀不再有声明。

### 调整

- 固定注册 `mc`、`x14ac`、`xr`、`xr2`、`xr3` 等 Office 前缀。
- 写回前根据工作表实际使用的限定名称清理 `mc:Ignorable`，避免悬空前缀。
- 增加回归断言，禁止工作表出现自动生成的 `ns*` 前缀，并核对 Ignorable 内容。

## 2026-09-15 — 图片尺寸与行高兼容修复

### 问题

- 同一单元格内的双端点锚点在不同客户端中会被重新解释，导致 WPS 图片比例变化、Excel 图片缩小，并可能产生行高变化的视觉结果。

### 调整

- 改用标准 `oneCellAnchor`，图片只依附起始单元格，不参与行列尺寸反算。
- 在锚点和 `a:xfrm` 中同时写入一致的宽高，避免客户端自行推断形状尺寸。
- 默认边距从 3 px 调整为 0 px，在保持纵横比的前提下最大化适配单元格。
- 增加锚点类型和显式变换尺寸回归测试。

## 2026-09-15 — WPS 行高锁定修复

### 问题

- WPS 样本行包含 `ht="75"`，但未包含 `customHeight="1"`。
- 移除 `DISPIMG` 公式后，WPS 会把该行重新视为自动高度并缩回默认值。

### 调整

- 对每个已转换图片所在行保留原 `ht`，并显式设置 `customHeight="1"`。
- 如果源行没有独立 `ht`，先固化当前工作表默认行高，再锁定，避免转换后自动重算。
- 增加行高锁定回归断言。

## 2026-09-15 — 原始列宽与双向居中修复

### 调整

- 转换过程不创建、不修改工作表列宽，完全沿用源文件列定义。
- 行高只保留并锁定源值，不根据图片反向调整。
- 修正 OOXML 字符列宽到像素的换算，加入 Excel/WPS 显示使用的 5 px 单元格补偿。
- 图片使用修正后的单元格宽高等比例缩放，并分别计算水平、垂直居中偏移。
- 增加源文件与输出文件行高、列宽一致性回归测试。

## 2026-09-15 — 原生格式双向写回

### 已完成

- WPS `DISPIMG/cellimages.xml` → Excel Rich Data 单元格图片。
- Excel Rich Data → WPS `DISPIMG/cellimages.xml` 单元格图片。
- 同步生成目标格式所需的 workbook relationships、Content Types、metadata/richData 或 cellimages relationships。
- 直接复用原媒体字节，加入转换前后 SHA-256 一致性测试。
- 新增 `native --target excel|wps` 命令。

### 验证边界

- 两个方向均可被共享解析器重新识别到同一 Sheet/Cell，ZIP 与 XML 结构完整。
- 下一步必须使用 Microsoft Excel 与 WPS 实机打开，确认各自原生“置于单元格”能力识别且无修复提示。

## 2026-09-15 — 全转换路径行高固化

### 问题与调整

- 原生 Excel/WPS 写回路径此前保留 `ht`，但未统一设置 `customHeight="1"`，目标客户端仍可能根据新单元格内容自动重算高度。
- 抽取统一行高固化函数，应用到通用浮动版、WPS → Excel 原生版、Excel → WPS 原生版。
- 已有 `ht` 原值保持不变；没有独立 `ht` 的行先固化转换前的工作表默认高度。
- 列宽不参与调整。
- 两个原生转换测试新增“转换前后 `ht` 相同且输出已锁定”断言。

## 2026-09-15 — 图片宽度越界修复

### 问题与调整

- Excel 与 WPS 对字符列宽中的 5 px 显示补偿解释不同，统一加补偿会使部分 WPS 自定义列宽中的图片越过右边界。
- 改用两端都安全的保守锚点宽度，不再把客户端显示补偿加入图片可用宽度。
- 最终宽高改为向下取整，并再次钳制到单元格可用宽高，消除浮点舍入造成的亚像素越界。
- 居中偏移仍基于最终钳制尺寸计算，行高和列宽 XML 均不修改。

## 2026-09-15 — 多图横竖比例适配

### 调整

- 将 Contain 等比例缩放抽取为统一计算规则，每张图片按自身比例及所在单元格尺寸独立处理。
- 横图以可用宽度为限制，竖图以可用高度为限制，正方形图片以单元格较短边为限制。
- 补齐 `sheetFormatPr.defaultColWidth` 读取；没有独立 `<col>` 定义的列不再一律假设 Excel 默认宽度。
- 行高、列宽保持源文件数值，不因图片比例变化而调整。
- 新增横图、竖图、正方形、边距、边界和双向居中单元测试。

## 2026-09-15 — 常见图片格式尺寸解析

### 调整

- 在不解码、不重压缩媒体的前提下，从文件头读取 PNG、JPEG、GIF、BMP、WebP 的原始像素宽高。
- JPEG 扩展支持基线、渐进及其他常见 SOF 标记；WebP 支持 VP8、VP8L、VP8X。
- 避免无法识别尺寸时将横图或竖图误判为正方形，保证多比例 Contain 计算正确。
- 增加五类图片头的尺寸解析回归测试。

## 2026-09-15 — 批量范围与转换日志

### 已完成

- 通用兼容版支持整个工作簿、指定工作表、指定 A1 区域三种范围。
- 范围筛选支持单个单元格及矩形区域，边界顺序可正向或反向。
- 部分转换时保留源格式全局图片索引，避免未选中图片引用失效；仅当某种来源图片全部完成转换时清理其索引。
- CLI 支持输出 JSON 日志，包含扫描、成功、失败、跳过、范围和完成时间。
- 原生格式写回仍限定整本转换，待实现安全的全局索引重排后再开放部分范围。

## 2026-09-15 — 浮动图片反向转换交互规则

### 已完成

- 嵌入单元格图片 → 浮动图片保持一键整本转换，不要求选区。
- 浮动图片 → 嵌入单元格图片强制要求工作表、A1 选区和目标格式（Excel/WPS）。
- 扫描标准 `oneCellAnchor` 与 `twoCellAnchor`，使用图片几何中心点判断归属单元格。
- 只转换中心点位于选区中的图片，转换成功后移除对应浮动锚点及不再使用的图片 relationship。
- 目标工作表中不存在实体单元格节点时自动按行列顺序创建。
- 已有目标格式单元格图片会与新转换图片一起重建索引，不被覆盖或遗失。
- 某个 Drawing 的图片全部转为单元格图片后，清理空 Drawing、关系文件、工作表引用和 Content Types 条目。

## 2026-09-15 — Excel/WPS 插件开发外壳

### 已完成

- 新增两端共用的任务窗格，提供“整本嵌入转浮动”“选区浮动转 Excel”“选区浮动转 WPS”三个入口。
- Excel 适配器通过 Office.js 获取活动工作表、选择区域及压缩工作簿副本。
- WPS 适配器通过 `Application` 获取活动工作簿、活动工作表和 Selection。
- 新增仅监听 `127.0.0.1` 的本地转换服务，复用 Python OOXML 核心，结果以新 XLSX 下载。
- 新增 Excel TaskPane 清单及 WPS `ribbon.xml/main.js` 外壳。

### 发布前事项

- 开发清单当前使用 localhost HTTP；正式分发需切换可信 HTTPS 地址。
- WPS 需按当前版本使用 `wpsjs publish` 打包发布。
- 需要在真实 Excel/WPS 中旁加载，核对文件读取权限、选区地址格式和下载行为。

### 验证

- JavaScript 及两份 XML 清单语法检查通过。
- 本地服务端到端 POST 验证通过：接收 Excel Base64 工作簿、转换 1 张图片、返回 106,340 字节有效 XLSX。

## 2026-09-15 — Windows 发布与 Git 交接准备

### 决策

- 正式交付平台确定为 Windows。
- 最终形态为 Excel、WPS 安装后可直接使用的插件，运行环境和后台转换组件随安装程序分发。
- 当前开发外壳不是最终安装包，不能对外宣称已完成安装交付。

### Git 边界

- 新增 `.gitignore`，排除用户工作簿、Excel 锁文件、转换输出、缓存、日志和本地构建目录。
- 源码、插件清单、测试及现有 README/交接记录纳入版本控制。
